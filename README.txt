SSML CHAT

RUN: npm install
THEN: npm start
OPEN: http://localhost:3000

Do not double-click index.html. The account system needs the Node server.
Sign-in/create UI is now independent from Socket.IO and connects to chat only after login.
Edit data/Gifs/gifs.js for GIFs. Edit data/Profile.js for profile settings.
